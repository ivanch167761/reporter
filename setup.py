from setuptools import setup, find_packages

setup(
        name='reporter', 
        version='1.0', 
        packages=find_packages(),
        description='Python tool for listing files in a project folder and creating reports (in Microsoft Word format), merging all information.',
        author='Ivan Kulikov',
        author_email='iva.kulikov@gmail.com',
    )
